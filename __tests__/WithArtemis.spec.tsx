import React from "react";

describe("withArtemis", () => {
    it("Calls getIntialProps if it is available", () => {
        console.log("test");
    });

    it("Rewinds the next.js Head", () => {
        // check to see if the function is called
        console.log("head");
    });

    it("Constructs an instance of the wrapped component with spreaded props", () => {
        console.log("component");
    });
});
